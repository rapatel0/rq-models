{{- if and .Values.hfToken (not .Values.existingHfTokenSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rotorquant.fullname" . }}-hf-token
  labels:
    {{- include "rotorquant.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.hfToken | quote }}
{{- end }}
